---
- name: "Don Dotfiles Host"
  hosts: localhost
  connection: local
  vars:
    neovim_nvim_version: stable
  roles:
    - base
    - neovim
    - babashka
  tags: base

- name: Workstation
  hosts: localhost
  connection: local
  become: false
  roles:
    - workstation
  tags: workstation
