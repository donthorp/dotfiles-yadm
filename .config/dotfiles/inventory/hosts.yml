---
all:
  children:
    managed:
      children:
        workstations:
          hosts:
            artemis01:
              ansible_host: artemis01
              ansible_connection: local
            artemis90:
              ansible_host: artemis90
              ansible_connection: local
            boros02:
              ansible_host: boros02
              ansible_connection: local
            boros03:
              ansible_host: boros03
              ansible_connection: local
        inference:
          hosts:
            vedfolnir00:
              ansible_host: vedfolnir00
              ansible_connection: local

    # Desktop environment groups — orthogonal to function groups.
    # A host belongs to exactly one DE group.
    gnome:
      hosts:
        artemis01:
        artemis90:
        boros02:
        boros03:
    cosmic:
      hosts:
        vedfolnir00:
