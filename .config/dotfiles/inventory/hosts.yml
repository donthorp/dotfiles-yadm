---
all:
  children:
    workstations:
      hosts:
        localhost:
          ansible_connection: local
    inference:
      hosts:
        # gpu-box-01:
        #   ansible_host: 192.168.1.x
        #   ansible_user: your-user
