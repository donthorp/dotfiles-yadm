---
all:
  children:
    managed:
      children:
        workstations:
          hosts:
            localhost:
              ansible_connection: local
        inference:
          hosts: {}
    gnome:
      hosts:
        localhost:
          ansible_connection: local
    cosmic:
      hosts: {}
